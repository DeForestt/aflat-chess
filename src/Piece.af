class Piece{
    contract{
        int x;
        int y;
        int color;
        adr face;
        adr selectedFace;

        adr board;
        adr draw;
        adr move;
    };

    int forceMove(int x, int y);
    int die();
};

int die@Piece(Piece piece){
    my.x = -1;
    my.y = -1;
    return 0;
};

int forceMove@Piece(int x, int y){
    my.x = x;
    my.y = y;
    return 0;
};