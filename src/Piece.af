.root "head"
.needs "Piece.gs"

int killPiece(Piece piece){
    piece.x = -1;
    piece.y = -1;
    return 0;
};

int forceMove(Piece piece, int x, int y){
    piece.x = x;
    piece.y = y;
    return 0;
};